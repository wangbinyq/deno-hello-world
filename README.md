# Deno Hello World 🦕

A minimal Hello World web server using **Deno's native HTTP API** (`Deno.serve`).

## Run

```bash
deno run --allow-net main.ts
```

Open http://localhost:8000 or http://localhost:8000/?name=YourName
